# niri（滚动平铺合成器）的配套包。
#
# niri / noctalia / mpvpaper 都不在 Ubuntu 归档里，由独立仓库
# code002-2/ubuntu-niri-repo 构建并发布成 apt 源（NIRI_REPO，见 rootfs.yml）。
# 其中 **niri 是硬性要求**，由 20-desktop.sh 的 Niri 分支直接 apt_install 并断言，
# 所以不写在这个列表里；noctalia / mpvpaper 走 best_effort。
#
# 其余是让 niri 在平板上真正能用起来的部分：niri 自己不提供状态栏、
# 启动器、通知、壁纸、屏幕键盘，这些都得单独装。
noctalia
mpvpaper
xwayland-satellite
fuzzel
waybar
mako-notifier
swaybg
swayidle
brightnessctl
playerctl
wl-clipboard
grim
slurp
kanshi
xdg-desktop-portal-gtk
xdg-desktop-portal-gnome
lxpolkit
gnome-keyring
# 触摸屏需要屏幕键盘。niri 支持虚拟键盘（见 niri issue #403：它的
# 键位绑定会忽略虚拟键盘的事件，反过来说明虚拟键盘是接进来的）。
# maliit-keyboard 用 zwp_input_method_v2，squeekboard 用 text-input-v3
maliit-keyboard
squeekboard
wtype
# 终端与输入法
foot
wl-clipboard
fcitx5
fcitx5-chinese-addons
fonts-noto-cjk
