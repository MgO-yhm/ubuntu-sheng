# niri（滚动平铺 Wayland 合成器）的配套包。
#
# niri 本体不在 Ubuntu 归档里（pool 下没有 n/niri，Launchpad 上也没有源包），
# 由 scripts/packages/build-niri.sh 从上游 vendored 源码构建成 deb，
# 经 _packages.yml 的 build-niri 作业产出 niri-deb 工件，
# 再由 rootfs.yml 收进 debs/ → 02-mount-chroot.sh 拷到 /tmp/debs。
# 所以这里**不写 niri**：它由 20-desktop.sh 的 Niri 分支装本地 deb 并断言。
#
# 其余是让 niri 在平板上真正能用起来的部分。niri 自己不提供状态栏、
# 启动器、通知、壁纸、屏幕键盘，这些都得单独装。
xwayland-satellite
fuzzel
waybar
mako-notifier
swaybg
swayidle
brightnessctl
playerctl
wl-clipboard
grim
slurp
kanshi
xdg-desktop-portal-gtk
xdg-desktop-portal-gnome
lxpolkit
gnome-keyring
# 触摸屏需要屏幕键盘。niri 支持虚拟键盘（见 niri issue #403：它的
# 键位绑定会忽略虚拟键盘的事件，反过来说明虚拟键盘是接进来的）。
# maliit-keyboard 用 zwp_input_method_v2，squeekboard 用 text-input-v3
maliit-keyboard
squeekboard
wtype
# 终端与输入法
foot
wl-clipboard
fcitx5
fcitx5-chinese-addons
fonts-noto-cjk
